"""Orpheus — local PII / sensitive-data scanner for Markdown files.

Fully offline. Uses a configurable local LLM backend (Ollama or any
OpenAI-compatible server) plus deterministic regex detectors.
"""

__version__ = "0.1.0"
